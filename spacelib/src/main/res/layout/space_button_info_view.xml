<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/white_ripple"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical">

    <!-- Здесь нужен именно ImageView,
    т.к. нажатие на ImageButton не будет считаться
    касанием по всей карточке. -->
    <ImageView
        android:id="@+id/contentIconImageView"
        android:layout_width="@dimen/space_item_icon_default_size"
        android:layout_height="@dimen/space_item_icon_default_size"
        android:layout_gravity="center"
        android:contentDescription="@string/menu_item" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/contentTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:lines="1"
        android:paddingTop="5dp"
        android:textSize="@dimen/space_item_text_default_size"
        tools:text="Label One" />

</LinearLayout>